import Sidebar from "../components/Sidebar";

const Dashboard = () => {
  return (
    <div className="admin-container">
      <Sidebar />
      <main>Dashboard</main>
    </div>
  );
};

export default Dashboard;
